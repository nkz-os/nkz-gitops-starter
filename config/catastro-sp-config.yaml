apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catastro-sp-config
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/nkz-os/gitops-config
    targetRevision: HEAD
    path: overlays/modules/catastro-sp
  destination:
    server: https://kubernetes.default.svc
    namespace: nekazari
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
