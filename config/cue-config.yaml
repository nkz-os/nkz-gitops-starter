apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cue-config
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/nkz-os/gitops-config
    targetRevision: HEAD
    path: overlays/modules/cue
  destination:
    server: https://kubernetes.default.svc
    namespace: nekazari
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
