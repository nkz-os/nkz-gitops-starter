apiVersion: v1
kind: ConfigMap
metadata:
  name: crop-health-config
  namespace: nekazari
  labels:
    app: crop-health-backend
    module: crop-health
data:
  CORS_ORIGINS: '["https://{{FRONTEND_DOMAIN}}"]'
  KEYCLOAK_URL: "https://{{KEYCLOAK_DOMAIN}}/auth"
